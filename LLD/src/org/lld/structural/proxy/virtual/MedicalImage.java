package org.lld.structural.proxy.virtual;

// this interface is used to provide medical image display
// we will provide actual service as well as proxy for actual service (medical image display)
public interface MedicalImage {
    void displayImage();
}